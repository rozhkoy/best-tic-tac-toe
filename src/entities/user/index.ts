import { userSlice } from '@/entities/user/store';
export { UserPanel } from './ui/userInfo';
export { userSlice } from '@/entities/user/store';
export const { updateUserInfo } = userSlice.actions;
export { UserEvents } from './lib/userEvents';
